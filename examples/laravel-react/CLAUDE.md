@vendor/scrapkit/engineering-kit/claude/CLAUDE.md

# Project guidelines

Project-specific instructions go here. Org-wide rules are imported above
from scrapkit/engineering-kit — do not remove the import line; on
`composer update` the imported rules follow the installed package version.
